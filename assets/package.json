{
  "scripts": {
    "deploy": "cd .. && mix assets.deploy && rm -f _build/esbuild && NODE_ENV=production tailwindcss --minify -i css/app.css -o ../priv/static/assets/app.css"
  },
  "dependencies": {
    "@tailwindcss/forms": "^0.3.4",
    "@tailwindcss/typography": "^0.4.1"
  },
  "devDependencies": {
    "tailwindcss": "^2.2.8"
  }
}
